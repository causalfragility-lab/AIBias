% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aib_build.R
\name{aib_build}
\alias{aib_build}
\title{Build an AIBias audit object}
\usage{
aib_build(data, id, time, group, decision, outcome = NULL, verbose = TRUE)
}
\arguments{
\item{data}{A data frame in long (panel) format.}

\item{id}{Character. Name of the unit identifier column.}

\item{time}{Character. Name of the time/wave column (integer or factor).}

\item{group}{Character. Name of the protected group column.}

\item{decision}{Character. Name of the binary decision column (0/1).}

\item{outcome}{Character or NULL. Optional downstream outcome column.}

\item{verbose}{Logical. Print validation messages. Default TRUE.}
}
\value{
An object of class \code{"aibias"}.
}
\description{
Constructs the core \code{aibias} S3 object from a panel dataset. Validates
the panel structure and prepares internal data for downstream analysis.
}
\details{
The function expects a balanced or unbalanced panel where:
\itemize{
\item \code{id} indexes units observed over multiple periods
\item \code{time} is an ordered index (will be coerced to integer rank)
\item \code{group} is a categorical variable indicating protected group membership
\item \code{decision} is a binary 0/1 variable (1 = favorable decision)
}
}
\examples{
data(lending_panel)
obj <- aib_build(lending_panel,
                 id       = "applicant_id",
                 time     = "year",
                 group    = "race",
                 decision = "approved")

}
