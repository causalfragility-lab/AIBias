% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aib_adjust.R
\name{aib_adjust}
\alias{aib_adjust}
\title{Covariate-adjusted bias trajectories}
\usage{
aib_adjust(
  object,
  formula,
  method = c("glm", "gam", "mixed"),
  ref_group = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{An \code{aibias} object from \code{\link[=aib_build]{aib_build()}}.}

\item{formula}{A one-sided formula specifying covariates, e.g.
\code{~ age + income + prior_score}. The group and time variables are
added automatically.}

\item{method}{One of \code{"glm"}, \code{"gam"}, or \code{"mixed"}.
\itemize{
\item \code{"glm"}: logistic regression
\item \code{"gam"}: generalized additive model (requires mgcv)
\item \code{"mixed"}: GLMM with random intercept for id (requires lme4)
}}

\item{ref_group}{Character. Reference group.}

\item{verbose}{Logical.}
}
\value{
The \code{aibias} object with \verb{$adjusted} populated, containing:
\itemize{
\item \code{trajectory}: adjusted bias trajectory
\item \code{marginal_rates}: marginal predicted rates by group and time
\item \code{model}: the fitted model object
\item \code{formula_used}: the full formula passed to the model
}
}
\description{
Estimates covariate-adjusted bias trajectories by fitting a model for
\eqn{Pr(D_{it} = 1 \mid X_{it}, A_i, t)} and computing marginal
predicted disparities by group and time.
}
\examples{
data(lending_panel)
obj <- aib_build(lending_panel, "applicant_id", "year", "race", "approved")
obj <- aib_adjust(obj, formula = ~ income + credit_score, method = "glm",
                  ref_group = "White")

}
