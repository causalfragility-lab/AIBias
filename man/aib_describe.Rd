% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aib_describe.R
\name{aib_describe}
\alias{aib_describe}
\title{Describe longitudinal disparity trajectories}
\usage{
aib_describe(object, ref_group = NULL, weights = NULL, verbose = TRUE)
}
\arguments{
\item{object}{An \code{aibias} object from \code{\link[=aib_build]{aib_build()}}.}

\item{ref_group}{Character. Reference group label. If NULL, uses the
first group level.}

\item{weights}{Numeric vector of time weights for cumulative burden.
Length must equal the number of time points. If NULL, equal weights.}

\item{verbose}{Logical. Print summary output.}
}
\value{
The \code{aibias} object with \verb{$bias} populated. The bias element is
a list with components:
\itemize{
\item \code{trajectory}: raw bias trajectory \eqn{B_{g,r}(t)}
\item \code{trajectory_smd}: standardized bias trajectory \eqn{B^*_{g,r}(t)}
\item \code{cumulative}: cumulative bias burden \eqn{CB_{g,r}(T)}
\item \code{slope}: first differences \eqn{\Delta B_{g,r}(t)}
\item \code{curvature}: second differences \eqn{\Delta^2 B_{g,r}(t)}
\item \code{ref_group}: reference group used
}
}
\description{
Computes group decision rate trajectories, raw and standardized bias
trajectories, and cumulative bias burden.
}
\examples{
data(lending_panel)
obj <- aib_build(lending_panel, "applicant_id", "year", "race", "approved")
obj <- aib_describe(obj, ref_group = "White")
obj$bias$cumulative

}
