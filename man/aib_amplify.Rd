% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aib_amplify.R
\name{aib_amplify}
\alias{aib_amplify}
\title{Compute dynamic bias amplification indices}
\usage{
aib_amplify(object, ref_group = NULL, verbose = TRUE)
}
\arguments{
\item{object}{An \code{aibias} object from \code{\link[=aib_build]{aib_build()}}. Recommended to run
\code{\link[=aib_transition]{aib_transition()}} first.}

\item{ref_group}{Character. Reference group.}

\item{verbose}{Logical.}
}
\value{
The \code{aibias} object with \verb{$amplification} populated. Contains:
\itemize{
\item \code{lagged_disparity}: \eqn{B_{g,r}(t|d)} for d equal to 0 or 1
\item \code{index}: Amplification index \eqn{A_{g,r}(t)}
\item \code{cumulative}: \eqn{A_{g,r}(T)} summed over time
\item \code{matrix_norm}: \eqn{\sum_t \|P_g(t) - P_r(t)\|}
}
}
\description{
Estimates the amplification index \eqn{A_{g,r}(t) = B_{g,r}(t|1) - B_{g,r}(t|0)},
which measures how conditioning on prior decision state changes the group
disparity at time \eqn{t}. Non-zero amplification indicates that prior
decisions are shaping current disparities—the hallmark of compounding bias.
}
\details{
A decision system exhibits \strong{bias amplification} if:
\enumerate{
\item \eqn{|B_{g,r}(t)| > |B_{g,r}(s)|} for some \eqn{t > s} (disparity grows), AND
\item \eqn{A_{g,r}(t) \neq 0} (prior decisions drive current disparity), OR
\item \eqn{P_g(t) \neq P_r(t)} (transition matrices are unequal)
}
}
\examples{
data(lending_panel)
obj <- aib_build(lending_panel, "applicant_id", "year", "race", "approved")
obj <- aib_transition(obj, ref_group = "White")
obj <- aib_amplify(obj, ref_group = "White")
obj$amplification$index

}
