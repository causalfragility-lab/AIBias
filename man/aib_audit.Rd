% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aib_audit.R
\name{aib_audit}
\alias{aib_audit}
\title{Run the full AIBias audit pipeline}
\usage{
aib_audit(
  object,
  ref_group = NULL,
  bootstrap = FALSE,
  B = 500,
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{An \code{aibias} object from \code{\link[=aib_build]{aib_build()}}, OR a data frame
(in which case \code{...} arguments are passed to \code{\link[=aib_build]{aib_build()}}).}

\item{ref_group}{Character. Reference group.}

\item{bootstrap}{Logical. Run bootstrap CIs? Default FALSE.}

\item{B}{Integer. Bootstrap replicates if \code{bootstrap = TRUE}.}

\item{seed}{Integer. Random seed.}

\item{verbose}{Logical.}

\item{...}{If \code{object} is a data frame, additional arguments passed
to \code{\link[=aib_build]{aib_build()}}.}
}
\value{
A fully-populated \code{aibias} object.
}
\description{
A convenience wrapper that runs the complete audit pipeline:
\code{aib_describe()} → \code{aib_transition()} → \code{aib_amplify()}.
Optionally runs \code{aib_bootstrap()} for uncertainty quantification.
}
\examples{
data(lending_panel)
result <- aib_audit(lending_panel,
                    id        = "applicant_id",
                    time      = "year",
                    group     = "race",
                    decision  = "approved",
                    ref_group = "White")
\donttest{
summary(result)
plot(result, type = "trajectory")
}

}
