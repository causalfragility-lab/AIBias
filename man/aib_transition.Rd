% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aib_transition.R
\name{aib_transition}
\alias{aib_transition}
\title{Compute group-specific decision transition matrices and disparities}
\usage{
aib_transition(object, ref_group = NULL, verbose = TRUE)
}
\arguments{
\item{object}{An \code{aibias} object from \code{\link[=aib_build]{aib_build()}}.}

\item{ref_group}{Character. Reference group. If NULL, uses first level.}

\item{verbose}{Logical. Print summary.}
}
\value{
The \code{aibias} object with \verb{$transitions} populated. Contains:
\itemize{
\item \code{probs}: Transition probabilities by group and time
\item \code{pooled}: Pooled transition probabilities (time-averaged)
\item \code{disparity}: Transition disparity \eqn{\Delta^{ab}_{g,r}(t)}
\item \code{recovery_gap}: Disparity in 0->1 transitions (recovery)
\item \code{retention_gap}: Disparity in 1->1 transitions (retention)
\item \code{matrices}: Named list of 2x2 transition matrices per group
}
}
\description{
Estimates group-specific Markov transition probabilities
\eqn{p_g^{ab}(t) = Pr(D_{it} = b \mid D_{i,t-1} = a, A_i = g)}
and derives transition disparities, advantage retention, and recovery gaps.
}
\examples{
data(lending_panel)
obj <- aib_build(lending_panel, "applicant_id", "year", "race", "approved")
obj <- aib_transition(obj, ref_group = "White")
obj$transitions$pooled

}
