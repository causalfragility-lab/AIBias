% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.aibias.R
\name{plot.aibias}
\alias{plot.aibias}
\title{Plot an aibias object}
\usage{
\method{plot}{aibias}(
  x,
  type = c("trajectory", "heatmap", "transition", "amplification"),
  show_ci = TRUE,
  color_palette = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{aibias} object.}

\item{type}{Character. Plot type. One of \code{"trajectory"}, \code{"heatmap"},
\code{"transition"}, \code{"amplification"}.}

\item{show_ci}{Logical. Show bootstrap CIs if available. Default TRUE.}

\item{color_palette}{Character vector of colors for groups. If NULL,
uses a sensible default.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot2} object.
}
\description{
Visualizes audit results. Supports four plot types:
\itemize{
\item \code{"trajectory"}: Bias trajectory \eqn{B_{g,r}(t)} over time
\item \code{"heatmap"}: Group-time disparity surface
\item \code{"transition"}: Group-specific transition probabilities
\item \code{"amplification"}: Amplification index \eqn{A_{g,r}(t)} over time
}
}
\examples{
\donttest{
data(lending_panel)
obj <- aib_audit(lending_panel,
                 id = "applicant_id", time = "year",
                 group = "race",     decision = "approved",
                 ref_group = "White", verbose = FALSE)
plot(obj, type = "trajectory")
plot(obj, type = "heatmap")
}

}
