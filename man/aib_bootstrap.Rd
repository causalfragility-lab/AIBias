% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aib_bootstrap.R
\name{aib_bootstrap}
\alias{aib_bootstrap}
\title{Bootstrap confidence intervals for bias trajectories}
\usage{
aib_bootstrap(
  object,
  B = 500,
  conf = 0.95,
  ref_group = NULL,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{An \code{aibias} object with \verb{$bias} populated.}

\item{B}{Integer. Number of bootstrap replicates. Default 500.}

\item{conf}{Numeric. Confidence level. Default 0.95.}

\item{ref_group}{Character. Reference group.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{verbose}{Logical.}
}
\value{
The \code{aibias} object with \verb{$bootstrap} populated.
}
\description{
Computes bootstrap confidence intervals for the bias trajectory
and cumulative burden by resampling units (cluster bootstrap).
}
\details{
Uses the cluster (unit-level) bootstrap to preserve the panel
structure. Units are resampled with replacement; all their
time observations are retained.
}
\examples{
\dontrun{
data(lending_panel)
obj <- aib_build(lending_panel, "applicant_id", "year", "race", "approved")
obj <- aib_describe(obj, ref_group = "White")
obj <- aib_bootstrap(obj, B = 200, seed = 42)
}

}
